import{describe,it,expect,vi}from'vitest';import{fetchGeneratedBackground,pollinationsUrl}from'./background';
describe('experimental background generation',()=>{it('builds a keyless vertical image request',()=>{const u=pollinationsUrl('neon forest');expect(u).toContain('image.pollinations.ai/prompt/');expect(u).toContain('width=212');expect(u).toContain('height=520');expect(u).not.toContain('key=')});it('fails cleanly when the public service refuses',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>new Response(JSON.stringify({message:'rate limited'}),{status:429,headers:{'content-type':'application/json'}})));await expect(fetchGeneratedBackground('x')).rejects.toThrow('rate limited');vi.unstubAllGlobals()})});
